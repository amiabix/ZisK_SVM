[package]
name = "hello_world"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
solana-program = "2.3"

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1

[target.bpf-unknown-unknown.dependencies]
solana-program = "2.3"
